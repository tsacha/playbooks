local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust


{% for user in postgres['users'] -%}
{% for ip in ['4','6'] %}
host {{ user.name }} {{ user.name }} {{ user['scope'+ip] }} md5
{% endfor %}
{% endfor %}